steps:
- name: 'gcr.io/cloud-builders/gcloud'
  entrypoint: 'bash'
  args:
  - '-eEuo'
  - 'pipefail'
  - '-c'
  - |-
    gsutil cp gs://drupal-artifact/app.zip .
    ls
    gcloud compute copy-files 10.154.0.2:./app.zip ./app.zip --zone=europe-west2-c --project "practiceproject-283720"